import { Routes } from '@angular/router';
import { SignUpComponent } from './pages/register/sign-up.component';

export const routes: Routes = [
  {
    path: 'sign-up',
    component: SignUpComponent,
  }
];
